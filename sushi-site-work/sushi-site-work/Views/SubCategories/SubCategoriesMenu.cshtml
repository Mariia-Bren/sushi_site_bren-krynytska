@model IEnumerable<sushi_site_work.Models.SubCategory>
<ul id="categories">
    @foreach (var subCategories in Model)
    {
        <li>
            @Html.ActionLink(subCategories.Name, "Browse", "SubCategories", new { SubCategory = subCategories.Name }, null)
    </li>
}
</ul>